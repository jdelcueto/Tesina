\ProvidesPackage{style}
%_______________________________________________________________________________
%Debes recordar, que todo trabajo científico debe incorporar:
    %Cubierta
    %Hoja en blanco
    %Portada interior, la cual debe llevar la misma información que en la cubierta del libro.
    %Índice General (con paginación)
    %Siglas y abreviaturas.
    %Fuentes utilizadas.
    %Bibliografía (puede ponerse en esta posición o detrás del texto)
    %Texto:
        %o Introducción
        %o Cuerpo de la tesina/tesis
        %o Conclusión
    %Índice de nombres / materias / citas bíblicas / particularizado (opcional)
%-------------------------------------------------------------------------------

%PAPER-------------------------------
%[Tamaño de papel]
%La tesina/tesis ha de estar escrita en un formato de papel DIN-A4 (21 cm x 29,7cm)
%-------------------------------------------------------------------------------
%Margins and Dimensions
\usepackage[a4paper,inner=2.5cm, outer=6.5cm, top=6cm, bottom=6cm,
marginparwidth=5cm, marginparsep=1cm]{geometry}

%\usepackage[letterpaper,inner=2cm, outer=2cm, top=2cm, bottom=3cm,
%marginparwidth=2cm, marginparsep=0cm]{geometry}

%LANGUAGE-----------------------------
\usepackage[main=spanish,english,greek]{babel}

%Bibliography Language
%\usepackage[fixlanguage]{babelbib}
%\selectbiblanguage{spanish}

%IMAGES------------------------------
\usepackage{graphicx}

%FONT---------------------------------
%Better font defaults
% import font loading packages
%\usepackage{fontspec}
%\usepackage{unicode-math}

% set up Termes (Times) and its companion math font
%\setmainfont[Ligatures=TeX,
%Extension=.otf,
%BoldFont=*-bold,
%UprightFont=*-regular,
%ItalicFont=*-italic,
%BoldItalicFont=*-bolditalic,
%SmallCapsFeatures={Letters=SmallCaps}]{texgyretermes}
%\setmathfont[Ligatures=TeX]{texgyretermes-math.otf}

% set up Heros (Helvetica)
%\setsansfont[Ligatures=TeX,
%Extension=.otf,
%BoldFont=*-bold,
%UprightFont=*-regular,
%ItalicFont=*-italic,
%BoldItalicFont=*-bolditalic,
%SmallCapsFeatures={Letters=SmallCaps}]{texgyreheros}

% More Math Symbols (therefore)
\usepackage{amsmath}
\usepackage{amssymb}

%Mathspec for math and fontspec for the rest
\usepackage{mathspec}
\usepackage[no-math]{fontspec}
%\usepackage{fontspec}
\usepackage{xltxtra,xunicode}

%Font: ARIAL UNICODE REPLACEMENT:TeX Gyre(http://www.gust.org.pl/projects/e-foundry/tex-gyre/)
%\setmainfont[
%    Extension=.otf,
%    UprightFont= *-regular,
%    BoldFont=*-bold,
%    ItalicFont=*-italic,
%    BoldItalicFont=*-bolditalic,
%    Ligatures={Common,TeX},
%    SmallCapsFeatures={Letters=SmallCaps}
%]{texgyreheros}

%\setmainfont[Ligatures=TeX]{Georgia}

% \setsansfont[
% Extension=.otf,
% UprightFont= *-regular,
% BoldFont=*-bold,
% ItalicFont=*-italic,
% BoldItalicFont=*-bolditalic,
% Ligatures={Common,TeX},
% SmallCapsFeatures={Letters=SmallCaps}
% ]{texgyreheros}

%\setsansfont[
%Path = ./font/,
%UprightFont = *-Regular,
%BoldFont = *-Bold,
%ItalicFont=*-Italic,
%BoldItalicFont=*-Bold-Italic,
%SmallCapsFont=arial-r,
%SmallCapsFeatures={Letters=SmallCaps},
%Ligatures={Common,TeX}]{Arial-Unicode}

\setsansfont[
Path= ./font/arial/,
%UprightFont=arialuni.ttf,
BoldFont=arialbd.ttf,
ItalicFont=ariali.ttf,
BoldItalicFont=arialbi.ttf,
]{arial.ttf}

%\setsansfont[Ligatures=TeX,
% BoldFont=Arial Unicode MS,
% BoldFeatures={FakeBold=3},
% ItalicFont=Arial Unicode MS,
% BoldItalicFont=Arial Unicode MS,
% BoldItalicFeatures={FakeBold=3},
% SmallCapsFeatures={Letters=SmallCaps}]{Arial Unicode MS}

\renewcommand{\familydefault}{\sfdefault}

%\setmathsfont(Digits,Latin)[Scale=MatchLowercase, Extension=.otf,
%UprightFont=*-regular, ItalicFont=*-italic, Ligatures=TeX]{texgyreheros}

\setmathsfont(Digits,Latin)[
Path= ./font/arial/,
%Scale=MatchLowercase,
%UprightFont=*-regular,
ItalicFont=ariali.ttf,
Ligatures=TeX]{arial.ttf}


%\setmathfont[Ligatures=TeX]{texgyretermes-math.otf}

%\AtBeginDocument{\addfontfeatures{Ligatures=Historic}}

\newfontfamily\greekfont[Scale=MatchUppercase,Ligatures=TeX]{Arial Unicode MS}

\renewcommand{\textgreek}[1]{\bgroup\greekfont{#1}\egroup} % Greek text



% Dialogue
%\RequirePackage{blkcntrl,moredefs,relsize}
%\usepackage{dialogue}
% MATH SYMBOLS
%\usepackage{mathtools}


%OLD ARIAL UNICODE MS OPTIONS
%\setmainfont[
%  Ligatures=TeX,
%  BoldFont=Arial Unicode MS,
%  BoldFeatures={FakeBold=3},
%  ItalicFont=Arial Unicode MS,
%  BoldItalicFont=Arial Unicode MS,
%  BoldItalicFeatures={FakeBold=3},
%]{Arial Unicode MS}
%\setmainfont[AutoFakeBold=2,AutoFakeSlant=0.25,Mapping=tex-text,Scale=1.0]{Arial
%    Unicode MS}

%PARAGRAPH----------------------------
%Line space
%\usepackage{leading}
%\leading{17.07165354402pt}
%\usepackage{leading}
%\leading{0.60cm}

\usepackage{setspace}
\setstretch{1.5}


%Paragraphs
\setlength{\parindent}{1.2cm}
\setlength{\parskip}{0.21cm}
%\setlength\parskip{\baselineskip}

%Disable Hyphenation
%\pretolerance=10000
%\tolerance=2000
%\emergencystretch=10pt

%Control Hyphenation of Compound Words
\usepackage[shortcuts]{extdash}

%Hyphenation Options
\hyphenation{WITT-GENS-TEIN ANS-COMBE TRAC-TA-TUS}
\tolerance=100
\hyphenpenalty=100
%Add Blank Pages
\usepackage{afterpage}

\newcommand\blankpage{%
    \null
    \thispagestyle{empty}%
    \addtocounter{page}{-1}%
    \newpage}


% URL

\usepackage{url}

    % By default the URLs are put in typewriter type in the body and the
    % bibliography of the document when using the \url command.  If you are
    % using many long URLs you may want to uncommennt the next line so they
    % are typeset a little smaller.
    % \renewcommand{\UrlFont}{\small\tt}
    \renewcommand{\UrlFont}{\rm}

% Margin notes
\usepackage{marginnote}


%todonotes for recollection
\usepackage[obeyDraft,textsize=small]{todonotes}

\newcommand{\engcitation}[1]{\todo[
    backgroundcolor=white,
    linecolor=gray,
    bordercolor=white
    ]{\emph{{\color[rgb]{0.5,0.5,0.5}#1}}}}

\newcommand{\pnote}[1]{\todo[
    backgroundcolor=green!25,
    linecolor=green!25,bordercolor=white
    ]{#1}}


%TITLES-----------------------
%Numbering Depth:
\setcounter{secnumdepth}{5}
\ifdraft{}{\renewcommand\thesubsubsection{\alph{subsubsection})}}
%Custom Titles
\usepackage[explicit]{titlesec}

%Los títulos de inicio del capítulo/sección centrados, en mayúsculas, y a 12pt
\titleformat{\chapter}
  {\normalfont\filcenter}
  {\thechapter}
  {12pt}
  {\MakeUppercase{#1}}
  [\vspace{1ex}%
  {\titlerule[2pt]}]

\titlespacing*{\chapter}
{0pt}{.42cm}{0.84cm}

%Los títulos del primer nivel irán en negrita y sin sangría

\titleformat{\section}
  {\normalfont\bfseries}
  {\thesection}{10pt}
  {#1}

\titleformat{name=\section,numberless}[block]
  {\normalfont\bfseries}
  {#1}
  {0pt}
  {}

\titlespacing*{\section}
{0pt}{0.42cm}{0.21cm}

%los del segundo nivel en negrita y con sangría de 0’5cm

\titleformat{\subsection}
  {\normalfont\bfseries}
  {\hspace{0.5cm}\thesubsection}{10pt}
  {#1}

\titleformat{name=\subsection,numberless}[block]
  {\normalfont\bfseries\hspace{0.5cm}}
  {#1}
  {0pt}
  {}

\titlespacing*{\subsection}
{0pt}{0.42cm}{0.21cm}

%los de tercer y cuarto nivel, irán sin negrita y con sangría de 0,5cm

\titleformat{\subsubsection}
  {\normalfont}
  {\hspace{0.5cm}\thesubsubsection}
  {10pt}
  {#1}

\titleformat{name=\subsubsection,numberless}[block]
  {\normalfont\hspace{0.5cm}}
  {#1}
  {0pt}
  {}

\titlespacing*{\subsubsection}
{0pt}{0.42cm}{0.21cm}

%----------------------------------------------------------------------


%CITING INSIDE THE BODY-------------
%Las citas literales de una obra, citadas en el cuerpo del texto, irán
%entrecomilladas
%\newcommand{name}[num][default]{definition}
%Macro: Cita literal interlineada
\newcommand{\citalitinterlin}[1]{\emph{<<#1>>}}

%excepto cuando tengan más de tres líneas, en cuyo caso será un
%párrafo independiente con una sangría de 1,2cm y opcionalmente en cursiva.
%Macro: Cita literal larga

\usepackage{changepage}   % for the adjustwidth environment

\newcommand{\citalitlar}[1]{
\begin{adjustwidth}{1.2cm}{}
%\emph{#1}
#1
\end{adjustwidth}
}

%\newenvironment{myquote}%
%  {\list{}{\leftmargin=1.2cm\rightmargin=0.3in}\item[]}%
%  {\endlist}

%  \newenvironment{myquotation}{\setlength{\leftmargini}{1.2cm}\quotation}{\endquotation}



%\newenvironment{myquotation}
 %               {\list{}{\listparindent=1.2cm \rightmargin=0cm \leftmargin=1.2cm%
  %                       \itemindent    \listparindent
   %                      \rightmargin   \leftmargin
    %                     \parsep        \z@ \@plus\p@}%
     %            \item\relax}
      %          {\endlist}

\renewenvironment{quotation}
               {\list{}{\listparindent=0cm%whatever you need
                        \itemindent    \listparindent
                        \leftmargin=1.2cm%  whatever you need
                        \rightmargin=0cm%whatever you need
                        \topsep=0cm%%%%%whatever you need
                        \parsep        \z@ \@plus\p@}%
                \item\relax}
               {\endlist}



%\newenvironment{quotation}
%                {\list{}{\listparindent 1.5em%
%                         \itemindent    \listparindent
%                         \rightmargin   \leftmargin
%                         \parsep        \z@ \@plus\p@}%
%                 \item\relax}
%                {\endlist}


%                \renewenvironment{quotation}[1][\leftmargin=1.2cm
%                \rightmargin=0cm] {\list{}{\listparindent 1.2em \itemindent
%                \listparindent \rightmargin \leftmargin \parsep \z@ \@plus\p@
%                #1}%
%            \item\relax} {\endlist}

\newenvironment{myquote}
                {\list{}{\rightmargin\leftmargin}%
                 \item\relax}
                {\endlist}

%-----------------------------------------------------------------------------

%ENGLISH WORDS---------
\babeltags{en = english}
\newcommand{\eng}[1]{\texten{\emph{#1}}}
%---------------------------------------------------------------------------
%\ifdraft{}{
%Fancy Headers
%\usepackage{fancyhdr}
%\setlength{\headheight}{36pt}
%\pagestyle{fancy}
%\fancyhead[LO,LE]{\fontsize{10}{12}\selectfont\leftmark}
%\fancyhead[RO,RE]{\fontsize{10}{12}\selectfont\rightmark}

%\fancypagestyle{fancythesis}{%
%    \fancyhf{}%
    % Note the ## here. It's required because \fancypagestyle is making a macro (\ps@fancybook).
    % If we just wrote #1, TeX would think that it's the argument to \ps@fancybook, but
    % \ps@fancybook doesn't take any arguments, so TeX would complain with an error message.
    % You are not expected to understand this.
%    \renewcommand*{\sectionmark}[1]{ \markright{\thesection\ ##1} }%
%    \renewcommand*{\chaptermark}[1]{ \markboth{\chaptername\ \thechapter: ##1}{} }%
    % Increase the length of the header such that the folios
    % (typography jargon for page numbers) move into the margin
%    \fancyhfoffset[LE]{6mm}% slightly less than 0.25in
%    \fancyhfoffset[RO]{6mm}%
    % Put some space and a vertical bar between the folio and the rest of the header
%    \fancyhead[LE]{\thepage\hskip3mm\vrule\hskip3mm\leftmark}%
%    \fancyhead[RO]{\rightmark\hskip3mm\vrule\hskip3mm\thepage}%
%}

%\pagestyle{fancythesis}
%}

%Text being revised
\definecolor{slightgrey}{RGB}{180,180,180}
\newenvironment{revision}{\color{slightgrey}\itshape}{}
\definecolor{addition}{RGB}{148,228,98}
\newenvironment{addition}{\color{addition}}{}
